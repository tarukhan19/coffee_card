import 'package:flutter/material.dart';

import 'home.dart';

/* Entry point of the Flutter application.
  MaterialApp is the root widget that provides material
  design structure.
  The 'home' property sets the initial screen of the app.
*/

void main() {

  runApp(
    MaterialApp(
      home: Home()
    )
  );
}
